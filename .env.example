# Optional overrides. Production site URL for metadata is defined in wrangler.jsonc → vars (single source of truth for Workers deploy).
# For local OpenNext builds that read .dev.vars, copy .dev.vars.example to .dev.vars and align NEXT_PUBLIC_SITE_URL with wrangler.jsonc.
NEXT_PUBLIC_SITE_URL=
DUMMYJSON_BASE_URL=
